# RippleFlow 技术文档索引

## 系统概述

**RippleFlow** 是一款面向创业软件开发团队的自用效率工具，核心定位为：
> **信息态势感知系统**——让团队成员快速掌握团队全局态势

### 核心价值

用户通过系统希望能够快速回答：

| 问题类型 | 系统能力 |
|----------|----------|
| **发生了什么？** | 全局索引、热点话题分析 |
| **和谁相关？** | 知识图谱、当事人识别 |
| **状态如何？** | 状态追踪、闭环检测 |
| **瓶颈在哪？** | 瓶颈自动识别、阻塞分析 |
| **如何解决？** | 决策记录、待办跟踪 |
| **需要什么资源？** | 资源需求提取、依赖分析 |
| **谁支持谁？** | 支持关系图谱、协作网络 |

### 传统价值延续

- **信息平权**：无论何时加入团队，均可获取历史决策与讨论上下文
- **知识沉淀**：对话自动结构化为 FAQ、决策日志、故障案例、参考手册
- **反复对齐消除**：LLM 驱动的智能问答，避免相同问题重复讨论
- **可信修正**：当事人可修正 LLM 总结，修正可选同步至原群
- **即时触达**：在群聊中直接@机器人查询，无需切换应用
- **主动运营**：AI 管家主动推送、提醒、发现价值，让知识库真正"活"起来

---

## 核心架构理念：策略与机制分离

### 设计哲学

RippleFlow 采用**数据面与控制面分离**的架构，将系统分为两部分：

```
┌─────────────────────────────────────────────────────────────────┐
│  知识平台（机制）              AI管家Agent（策略）               │
│  ───────────────              ───────────────                  │
│  • 稳定、可靠                  • 灵活、智能                      │
│  • 版本发布更新                • 在线实时优化                    │
│  • 需要开发部署                • 提示词即可调整                  │
│  • 低频变更                    • 高频迭代                        │
│  • 关注正确性                  • 关注效果                        │
└─────────────────────────────────────────────────────────────────┘
```

**核心价值**：策略可以独立演化，不影响机制稳定性。

### 策略演化的完整生命周期

```
┌─────────────────────────────────────────────────────────────────┐
│                    策略全生命周期（无需代码）                     │
│                                                                 │
│  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐  │
│  │  定义   │ ──→ │  执行   │ ──→ │  验证   │ ──→ │  优化   │  │
│  │ Routine │     │ 工具调用 │     │ 观察效果 │     │ 版本迭代 │  │
│  └─────────┘     └─────────┘     └─────────┘     └─────────┘  │
│       ↑                                               │        │
│       └───────────────────────────────────────────────┘        │
│                         持续迭代                                 │
│                                                                 │
│  三种来源：                                                     │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐       │
│  │ 预设例程      │  │ 业务新增      │  │ 自省改进      │       │
│  │               │  │               │  │               │       │
│  │ • 授权升级    │  │ • 新类别      │  │ • 时机优化    │       │
│  │ • 待办提醒    │  │ • 新任务类型  │  │ • 规则调整    │       │
│  │ • 每日快报    │  │ • 新触发条件  │  │ • 内容优化    │       │
│  └───────────────┘  └───────────────┘  └───────────────┘       │
│                                                                 │
│  全部通过提示词实现，无需固化代码                               │
└─────────────────────────────────────────────────────────────────┘
```

### Agent = 感知 + 决策 + 执行 + 自省

```
┌─────────────────────────────────────────────────────────────────┐
│                      AI 管家 Agent                              │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                     感知模块                               │  │
│  │  监听平台事件 → 理解当前状态 → 识别行动机会               │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              ↓                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                     决策模块                               │  │
│  │  规则引擎（Routine脚本） + LLM智能层                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              ↓                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                     执行模块                               │  │
│  │  工具调用 → 平台API → Function Call                       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              ↓                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                     自省模块                               │  │
│  │  观察结果 → 反思效果 → 沉淀经验 → 优化策略                 │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 这个设计的深远意义

```
传统系统：代码 = 逻辑 = 固化
    ↓
RippleFlow：代码 = 机制 = 稳定
            提示词 = 策略 = 演化

Agent 从「执行者」变成「运营者」
系统 从「被动响应」变成「主动进化」
```

**AI管家模拟一个超强的人类运维**——人类通过经验总结优化工作方法，AI管家通过自省优化Routine脚本和决策策略。

---

## 设计约束
| 约束 | 说明 |
|------|------|
| 聊天工具 | 自研，通过 Webhook 推送消息，机器人可回复但不主动发群 |
| 机器人客户端 | 聊天机器人作为 RippleFlow 第二客户端，支持群内自然语言查询 |
| AI 管家 | 独立的智能运营角色，可主动发群推送提醒和快报 |
| 认证 | 企业 LDAP + SSO，系统只做白名单管理 |
| LLM | 公司内部部署模型，全中文场景优化 |
| 存储 | **SQLite（轻量）或 PostgreSQL（高并发）**，全文检索 + LLM 直接处理 |
| 敏感信息 | 发布前需全部当事人明确授权，pending 状态 7 天后升级管理员 |
| 检索策略 | 全文检索召回候选 + LLM 基于摘要综合回答（非语义向量检索） |
| 时间窗口 | 默认 3 个月滑窗（技术决策、参考信息永久有效） |

---

## 文档清单

| 编号 | 文档 | 说明 | 用途 |
|------|------|------|------|
| 01 | [系统架构](./01_system_architecture.md) | 组件图、模块边界、数据流、技术栈 | SDD |
| 02 | [数据库 DDL (PostgreSQL)](./02_database_ddl.sql) | 完整 PostgreSQL 建表语句 | SDD / 开发 |
| 02b | [数据库 DDL (SQLite)](./02b_database_ddl_sqlite.sql) | 轻量级 SQLite 建表语句 | SDD / 开发 |
| 03 | [API 规范](./03_api_reference.yaml) | OpenAPI 3.0，全部 RESTful 接口 | SDD / TDD / E2E |
| 04 | [服务层接口](./04_service_interfaces.md) | Python Protocol 定义，供 UT mock | TDD |
| 05 | [E2E 测试目录](./05_e2e_test_catalog.md) | 用例清单（函数调用式）+ Playwright 结构 | ST / E2E |
| 06 | [LLM Prompt 模板库](./06_llm_prompt_templates.md) | Stage 0–5 + Q&A + 纪要生成 + 管家 Prompt | 开发 / 调优 |
| 07 | [单元测试目录](./07_unit_test_catalog.md) | 服务层 UT 用例清单，pytest + AsyncMock | TDD |
| 08 | [AI 管家演化架构](./08_ai_butler_architecture.md) | 权限层级、自我演化机制、主动服务清单 | SDD |

---

## 信息类别体系（系统默认 9 类）

| 代码 | 名称 | 典型内容 | 时间窗口 |
|------|------|----------|----------|
| `tech_decision` | 技术决策 | 架构选型、方案确认 | 永久 |
| `qa_faq` | 问题解答 | 技术问答、排查方法 | 90 天 |
| `bug_incident` | 故障案例 | Bug 报告、复盘记录 | 90 天 |
| `reference_data` | 参考信息 | IP、URL、端口、账号名 | 永久 |
| `action_item` | 任务待办 | @人名 + 任务指派 | 30 天 |
| `discussion_notes` | 讨论纪要 | 会议总结、共识记录 | 90 天 |
| `knowledge_share` | 知识分享 | 技术文章、经验分享 | 180 天 |
| `env_config` | 环境配置 | 部署步骤、配置说明 | 永久 |
| `project_update` | 项目动态 | 发布公告、里程碑 | 180 天 |

---

## 客户端入口

RippleFlow 提供三个客户端入口：

### 1. Web Dashboard（完整功能）

面向深度使用场景，提供完整的知识库管理能力：

| 功能模块 | 说明 |
|----------|------|
| 知识库浏览 | 按类别、时间、标签筛选话题线索 |
| 搜索问答 | 自然语言提问，返回答案 + 来源 |
| 敏感授权 | 处理待授权的敏感内容请求，实时查看授权进度 |
| 当事人修改 | 修正摘要内容，可选同步回群 |
| 个人贡献 | 查看个人知识贡献统计 |
| 管理后台 | 白名单、类别管理、统计面板（管理员） |

### 2. 聊天机器人（即时查询）

面向轻量查询场景，在群聊中直接@机器人：

```
用户：@机器人 Redis 连接池怎么配置
机器人：找到 2 条相关记录...

用户：@机器人 我有什么待办
机器人：您有 3 个待办任务...

用户：@机器人 生成今天产品群的会议纪要
机器人：已生成纪要，点击查看详情...
```

**机器人支持的查询类型**：
- 知识库搜索/问答
- 个人待办查询
- 参考数据查询（IP、URL、配置）
- 会议纪要生成（可指定时间范围）

### 3. AI 管家（平台灵魂）

AI 管家是 RippleFlow 的"灵魂"，通过**感知-决策-执行-自省**循环持续运营：

```
感知层：监控所有平台事件（消息、问答、待办、订阅、用户行为...）
    ↓
决策层：判断事件相关性、行动必要性、执行方式
    ↓
执行层：发送通知 | 生成报告 | 创建任务 | 回复问答
    ↓
自省层：分析效果 | 提取模式 | 沉淀最佳实践 | 优化提示词
```

**AI 管家核心职责**：

| 职责 | 说明 |
|------|------|
| 信息平权 | 确保信息公平触达每个需要的人 |
| 智能推荐 | 发现有价值的信息并推荐给相关人员 |
| 总结提炼 | 将复杂信息转化为易理解的形式 |
| 问答辅助 | 帮助用户快速找到答案 |
| 任务跟踪 | 跟踪任务进度，确保不遗漏 |
| 及时提醒 | 在关键时刻提醒相关人员 |

**AI 管家核心技能**：
- 数据可视化：自动识别群聊数据，生成图表
- 链接摘要：自动获取链接内容并生成摘要
- 任务识别：从群聊消息中识别任务并创建待办
- 偏好学习：记住用户偏好，自动应用到后续服务

---

## AI 管家架构（v2.0）

### 提示词分级架构

```
butler_prompts/
├── core/          # 冷启动核心提示词（不可修改）
├── duties/        # 职责定义（管家可优化）
├── skills/        # 技能模板（管家可优化）
├── insights/      # 自省沉淀（管家维护）
└── extensions/    # 扩展脚本（零代码新功能）
```

### 自省与平台迭代

| 周期 | 内容 |
|------|------|
| 每日自省 | 回顾行为效果，提取模式 |
| 每周复盘 | 汇总数据，优化职责 |
| 月度评估 | 平台整体评估，改进建议 |

AI 管家维护自己的经验知识库，持续优化服务质量：

```
butler_experience_memory:
  - 高峰使用时段: "周一上午问答量大"
  - 常见问题模式: "Redis配置相关占比30%"
  - 快报最佳发送时间: "周一9:30打开率最高"
  - 待办完成率: "提醒后完成率提升40%"
  - 问答满意度: "本月平均4.2/5分"
```

---

## 角色权限矩阵

| 能力 | 普通成员 | 当事人 | 管理员 |
|------|----------|--------|--------|
| 查看知识库 | ✅ | ✅ | ✅ |
| 搜索/问答 | ✅ | ✅ | ✅ |
| 提交问答反馈 | ✅ | ✅ | ✅ |
| 查看个人贡献统计 | ✅ | ✅ | ✅ |
| 修改所参与的话题线索 | ❌ | ✅ | ✅ |
| 敏感内容授权（仅本人） | ❌ | ✅ | ✅ |
| 触发纪要生成 | ✅ | ✅ | ✅ |
| 白名单管理 | ❌ | ❌ | ✅ |
| 类别管理 | ❌ | ❌ | ✅ |
| 强制归档/删除 | ❌ | ❌ | ✅ |
| 管理员介入敏感授权 | ❌ | ❌ | ✅ |
| 配置 AI 管家任务 | ❌ | ❌ | ✅ |

---

## 模块边界

```
┌───────────────────────────────────────────────────────────────────────┐
│  客户端层                                                              │
│                                                                       │
│  ┌─────────────────────┐  ┌───────────────┐  ┌───────────────────┐   │
│  │  Web Dashboard      │  │ 聊天机器人    │  │  AI 管家          │   │
│  │  (Vue 3)            │  │ (已有系统)    │  │  (主动推送)       │   │
│  │  - 知识库浏览        │  │ - 自然语言查询│  │ - 每周快报        │   │
│  │  - 搜索问答          │  │ - 待办查询    │  │ - 到期提醒        │   │
│  │  - 敏感授权          │  │ - 参考数据    │  │ - 健康报告        │   │
│  │  - 个人贡献          │  │ - 纪要生成    │  │ - 反馈收集        │   │
│  │  - 管理后台          │  │               │  │ - 自主学习        │   │
│  └─────────┬───────────┘  └───────┬───────┘  └─────────┬─────────┘   │
└────────────┼─────────────────────┼────────────────────┼──────────────┘
             │ HTTPS/REST          │ POST /api/v1/bot   │ 主动推送
             ▼                     ▼                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│  API Layer (FastAPI)                                                   │
│                                                                       │
│  ┌──────────────┐  ┌─────────────────┐  ┌───────────────────────┐    │
│  │ AuthRouter   │  │ WebhookRouter   │  │ APIv1Router           │    │
│  │ /auth/*      │  │ /webhook/*      │  │ /api/v1/*             │    │
│  └──────────────┘  └─────────────────┘  └───────────────────────┘    │
│                                       ┌───────────────────────┐       │
│                                       │ ButlerRouter          │       │
│                                       │ /api/v1/butler/*      │       │
│                                       └───────────────────────┘       │
│  Middleware: JWTAuthMiddleware / RateLimitMiddleware / LogMiddleware   │
└──────────────────────────┬────────────────────────────────────────────┘
                           │
          ┌────────────────┼────────────────────┐
          ▼                ▼                    ▼
┌─────────────────────┐  ┌──────────────────┐  ┌──────────────────────┐
│  Service Layer      │  │  内存缓存/Redis  │  │  LLM API             │
│                     │  │                  │  │                      │
│  MessageService     │  │  - Celery 队列    │  │  (公司内部部署)       │
│  ThreadService      │  │  - 会话 Token     │  │                      │
│  SearchService      │  │  - 搜索缓存       │  │                      │
│  SensitiveService   │  │  - 管家经验库     │  │                      │
│  AuthService        │  └──────────────────┘  └──────────────────────┘
│  NotificationService│
│  FeedbackService    │  ← 新增：问答反馈
│  BotAdapterService  │
│  AIButlerService    │  ← 新增：AI 管家
│  ChatToolService    │
│  AdminService       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  PostgreSQL                                                          │
│  messages / topic_threads / sensitive_authorizations                │
│  thread_modifications / reference_data_items                       │
│  user_whitelist / category_definitions / notifications              │
│  qa_feedback / butler_tasks / butler_experience  ← 新增             │
└─────────────────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Celery Workers (独立进程)                                            │
│  ProcessingPipeline / SummaryUpdateWorker / NotificationWorker       │
│  ReminderScheduler / SyncToChatWorker / EscalationWorker             │
│  ButlerScheduler / WeeklyDigestWorker / ActionItemReminderWorker     │
│  ← 新增管家相关 Worker                                                │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 0.1 | 2026-02-28 | 初版，基于需求讨论整理 |
| 0.2 | 2026-02-28 | 新增 06_llm_prompt_templates.md |
| 0.3 | 2026-02-28 | 新增 07_unit_test_catalog.md |
| 0.4 | 2026-03-01 | 新增机器人客户端架构、敏感授权升级机制 |
| 0.5 | 2026-03-01 | 新增 AI 管家角色、问答反馈机制、每周快报、待办提醒 |